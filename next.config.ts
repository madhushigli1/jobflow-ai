import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hide the floating Next.js dev indicator — it clashes with the editorial UI.
  devIndicators: false,
};

export default nextConfig;
